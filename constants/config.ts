const configs = {
  APP_ENV: `${process.env.NEXT_PUBLIC_API_BASE_URL}`,
  API_DOMAIN: `${process.env.NEXT_PUBLIC_API_BASE_URL}`,
  AWS_DOMAIN: `${process.env.NEXT_PUBLIC_AWS_DOMAIN}`,
};

export default configs;
